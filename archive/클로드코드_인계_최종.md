# 프로젝트 인계 — 클로드 코드용

## 프로젝트 개요

서울시 아파트 입지 특성을 반영한 주택담보대출 리스크 평가 모델. 파일럿: 동대문구 단독.
정부 LTV 규제 상한선은 그대로 두고, 그 안에서 은행이 담보물의 공간적 리스크를 반영해
실행 LTV를 정교화하는 모델. 실수요자 관점 → 예측은 회귀형. 리스크 = 담보가치의
변동성(volatility)+유동성(liquidity) 리스크(위험도 개념 아님).

## 로컬 데이터 파일

- **`final.csv`** — 동대문구 317개 단지 × 28개 분기(2019Q1~2025Q4) 패널, 8,876행×48컬럼.
  100세대 K-apt 필터를 버리고 실거래가 원본에 실제 존재하는 모든 단지로 재구성한 최신
  버전. 상권지수 2종, 학군(school_pc1), 인구(population/living_population/
  pop_growth_mom), 경사도(100/150/200/300m 반경×6통계=24컬럼), 교통(지하철 관련 5컬럼),
  건물특성(buildYear/building_age) 포함. 자세한 컬럼 정의는 `데이터_명세서.pdf` 참고
  (첨부된 문서 형태로 별도 보유 중이면 같이 전달할 것)
- **`master_19_25_cleaning.csv`** (또는 동일 계열의 실거래 원본) — 2019~2025 동대문구
  실거래 마스터, 316개 단지, 15,365건, 일별 계약일(contract_date) 포함. XGBoost 예측
  트랙에서 월별로 집계해 사용 중.

## final.csv 관련 확정된 처리 방침 (바로 반영할 것)

1. **building_age 재계산 필요**: 현재 "2026년 기준" 고정값으로 전 분기 동일하게 들어가
   있음. `yyyymm의 연도 - buildYear`로 분기별 재계산할 것.
2. **경사도 4개 반경(100/150/200/300m) 중 하나 선택**: 넷을 동시에 모델에 넣지 말 것
   (다중공선성 위험). 각 반경으로 각각 실행해보고 비교해서 선택하는 방식으로 진행.
3. **상권지수 두 버전 중 `commercial_index_lasso19` 우선 검토**: `commercial_index`(엔트로피
   기반)는 가격과의 인과관계가 검증 안 된 지표, `commercial_index_lasso19`는 GroupKFold로
   검증해 단지 고정효과 제거 후 가격편차 R²=0.132 확인된 지표.
4. **분기 단위 유지가 맞는 설계**: 원천 데이터가 애초에 분기 단위로만 존재하므로, 더 잘게
   쪼개도 정보량이 늘지 않고 오히려 의사복제(pseudo-replication) 문제만 생김. 시간 단위를
   억지로 세분화하지 말 것.

## 방법론 — 두 축으로 분리 운영 (혼동 금지)

| | 트랙 A: GWR + LASSO/ElasticNet | 트랙 B: XGBoost |
|---|---|---|
| 질문 | **왜** 지금 이 단지/분기가 위험한가 (설명) | **앞으로** 어떻게 될까 (예측) |
| 데이터 | final.csv, 단지×분기 패널 그대로 | 실거래 원본을 월별 집계 |
| 검증 | **GroupKFold** (단지 단위 완전분리) | **walk-forward** (시간 기준 분할, 무작위 분할 절대 금지) |

두 트랙에 들어가는 입지변수의 정의(계산식)는 반드시 동일하게 유지 — "GWR/LASSO에서
중요했던 변수가 XGBoost importance에서도 상위인가" 교차검증이 이후 단계에서 필요하기
때문. 단, 표본 구성·검증 방식·피처 가공은 트랙별로 독립적으로 설계해도 무방.

### 검증 방식이 다른 이유 (자기상관 원칙)

자기상관은 데이터 분할 때문에 생기는 게 아니라 원래 존재하는 속성(같은 단지의 여러
분기, 또는 인접 시점 값들이 서로 유사한 것)이며, 분할 방식은 이를 무시하고 섞느냐
감안해서 나누느냐만 결정한다. 이전에 상권지수(commercial_index_lasso19) 개발 중
일반 K-fold로는 alpha가 폴드마다 불안정하게 흔들리는 문제가 실제로 발생했고
(19개↔28개 카테고리 선택 오락가락), GroupKFold(단지 단위 완전분리)로 전환 후
alpha=0.91에 안정적으로 수렴한 전례가 있음. XGBoost도 같은 원리로, 시간 자기상관
(미래정보 누수)을 막기 위해 반드시 walk-forward만 사용.

### 트랙 A: LASSO/ElasticNet 다음 단계

- 지금까지는 상권 카테고리 30개 내에서만 변수선택(commercial_index_lasso19)을 완료한
  상태. 다음 단계는 전체 변수(상권 결과물+학군+인구+경사도+교통+건물특성)를 한 번에
  투입해 다시 변수선택 + 리스크 스코어 산출.
- 교차검증은 반드시 GroupKFold(단지 단위) 원칙 유지.
- `school_pc1`은 "값이 클수록 리스크 낮음"으로 다른 변수들과 부호가 반대이므로, 최종
  리스크 스코어 합산 시 부호 통일(반전) 필요.

### 트랙 B: XGBoost 처리 방침

- **시간단위 처리 (옵션 A 확정)**: 월별 구조를 유지하고, final.csv의 분기 입지특성값을
  해당 분기에 속한 각 월에 그대로 복제(broadcast)해서 붙일 것. 분기 단위로 통째로
  전환하지 말 것(표본 수가 더 줄어들어 이미 겪고 있는 표본부족 문제가 악화됨).
- 베이스라인(기존 피처만: 전용면적, 건축연차, 이웃가격/모멘텀, 정책경과일) vs 확장버전
  (+입지특성)을 비교. 입지특성은 44개 컬럼 전부가 아니라 LASSO/ElasticNet이 선별한
  변수 위주로 투입 (다중공선성 방지, 트랙 A와의 교차검증도 자연히 겸함).
- XGBoost는 시계열 모델이 아니라 일반 테이블 모델이며, 시간정보를 압축한 피처(이웃모멘텀,
  정책경과일 등)와 walk-forward 검증 강제라는 두 장치로 시계열 예측처럼 동작하도록
  설계된 것임을 유의 (피처 설계·해석 시 이 전제를 벗어나지 않을 것).

## 선행지표 실험 현황 (트랙 B 한정)

- **금리(기준금리, 경과변화율)**: raw/게이팅 두 버전 다 시도했으나 walk-forward 가중
  지표 기준 베이스라인을 넘지 못함. 원인은 "국면마다 금리-가격 관계가 다르게 작동하는데
  표본이 적어 학습이 어려움"으로 판단 → **트랙 B에서 제외 확정**. 크롤링한 데이터 자체는
  보존 (트랙 A의 국면별 비교 등에서 재사용 가능성 있으니 삭제하지 말 것).
- **매매수급동향지수**: level/change_raw/change_gated 세 버전 시도. level 버전이 가중
  평균 기준 소폭 개선되었으나, 폴드 4개 중 3개는 악화되고 1개(회복기 국면)만 크게 개선되어
  전체 평균을 끌어올린 형태 — "폴드 대부분에서 고르게 개선"이라는 판단기준에 미달.
  **아래 폴드 재설계 이후 재평가 예정, 현재는 채택/폐기 보류 상태.**
- 개선 판단 기준: 가중평균 지표 하나만으로 판단하지 말고 반드시 폴드별 표로 확인.
  특정 폴드 하나의 개선이 전체를 끌어올리는 경우는 "개선"으로 인정하지 않음.

## 검증 폴드 재설계 (다음 우선 작업)

기존 폴드가 달력 기준 순차분할이라 폴드 하나의 우연한 결과가 전체 판단을 왜곡하는 문제가
반복 발생. 아래 원칙으로 재설계할 것:

- 가격/거래량 시계열(월별 집계) 자체의 모멘텀 부호 전환만 보고 독립적으로 폴드 경계를
  재설정. **어떤 피처(금리·매매수급동향 등) 테스트 결과에도 영향받지 말 것** — 특정
  피처가 잘 나온 구간을 살리는 방향으로 조정하는 것은 평가 기준을 결과에 맞춰 사후
  조정하는 것과 같으므로 금지.
- 부호 전환은 최소 2개월 이상 지속돼야 확정된 전환으로 인정 (노이즈 필터).
- 폴드 재설계 후 베이스라인·금리·매매수급동향 전부 동일 기준으로 재평가.

## 실행 순서 제안

1. 폴드 재설계 (가격 시계열 기반)
2. final.csv의 building_age 재계산, 경사도 반경 선택
3. 분기 입지특성 → 월별 broadcast 결합 (XGBoost 트랙용)
4. LASSO/ElasticNet 전체변수 투입 + 리스크 스코어 산출 (GroupKFold)
5. XGBoost 베이스라인 vs 입지특성 확장버전 비교 (재설계된 폴드, walk-forward)
6. 매매수급동향 재평가 (재설계된 폴드로)
7. 트랙 A(LASSO 선별변수) vs 트랙 B(XGBoost importance) 교차검증
8. GWR 국지 회귀계수 산출, 리스크 스코어 → LTV 매핑 로직 설계
