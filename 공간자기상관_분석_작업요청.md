# 공간자기상관 분석 작업 요청 (이웃단지 개수 k 근거 마련)

## 목적

GWR·XGBoost 두 트랙에서 공통으로 쓰는 "단지 + 최근접 이웃 k개 평균" 스무딩 방식의 k값을
임의로 정하지 않고, 실거래 데이터의 공간자기상관 구조로부터 통계적 근거를 마련한다.
(경사도 반경 100/150/200/300m 선택 작업과 같은 성격 — 하이퍼파라미터를 실증적으로 검증)

## 반드시 지킬 원칙 — 시장 전체 사이클 오염 제거

**이 분석에서 가장 중요한 전제조건.** 원본 가격을 그대로 쓰면, 동대문구 전체가 같은
시기에 동시에 오르내렸던 시장 사이클(2020년 급등, 2022년 급락, 2025년 재과열 등) 때문에
물리적으로 멀리 떨어진 단지끼리도 "같은 시점에 같이 움직였다"는 이유만으로 상관관계가
높게 나올 수 있다. 이러면 진짜 공간적 근접성에 의한 상관을 시장 전체 흐름이 가려버려서
k를 실제보다 과대추정하게 된다.

**따라서 상관분석 전에 반드시 시점 고정효과를 제거할 것**: 각 월의 동대문구 전체 평균
가격 대비 편차(잔차)로 변환한 뒤, 그 잔차값으로 분석을 진행한다.

```
price_residual = price_per_m2 - (해당 year_month의 전체 평균 price_per_m2)
```

## 분석 절차

1. 개별 거래 단위가 아니라 **단지×월별로 집계한 가격지수**를 사용할 것 (개별 거래는
   노이즈가 심함 — 특히 월 1건 이하인 단지가 많으므로 집계값을 써야 함)
2. 위 방식으로 시점 고정효과를 제거해 잔차 시계열을 만들 것
3. 모든 단지쌍에 대해 (물리적 거리, 잔차 간 상관계수)를 계산
4. 거리를 구간별로 나누어(예: 100m 단위) 구간별 평균 상관계수를 그려 공간 상관도표
   (spatial correlogram)를 작성
5. 상관계수가 유의미한 수준으로 유지되는 거리 구간을 확인하고, 그 거리 안에 평균적으로
   몇 개의 이웃 단지가 포함되는지를 근거로 k값을 도출

## 참고 — 예상되는 결과 형태

이전에 GWR을 동대문구 단독 표본으로 실행했을 때, 최적 대역폭이 표본 거의 전체로
수렴한 전례가 있음(자치구 하나만으로는 거리에 따른 공간적 이질성이 뚜렷하게 드러나지
않는 경향). 이번 분석도 상관계수가 거리에 따라 뚜렷하게 꺾이는 지점 없이 완만하게
감소하는 형태로 나올 가능성이 있다. 이는 분석 실패가 아니라 일관된 발견이므로, 뚜렷한
elbow가 안 보이더라도 그 자체를 결과로 기록할 것 (예: "이 정도 반경까지는 상관이
특정 수준 이상 유지된다"는 완만한 기준으로 k를 정하는 방식도 가능).

## 결과물 형식

- 거리 구간별 평균 상관계수 표(또는 시각화)
- 선택한 k값과 그 근거(어느 거리 기준으로 정했는지, 그 거리 안 평균 이웃 수)
- 시점 고정효과 제거 전/후 상관관계가 어떻게 달라졌는지 비교 (오염 제거 효과 확인용)
