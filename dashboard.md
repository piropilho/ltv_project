# LTV 데모 대시보드 — 구현 가이드

## 데모 흐름

1. 동대문구 지도에서 지역(동) 클릭
2. 해당 지역 아파트 단지 리스트업
3. 단지 선택 → 평균 매매가 확인
4. 희망 대출금액 입력
5. 해당 단지의 가격 변동성 + 유동성 예측 코멘트, 제안 LTV·대출가능금액 표시

## 이미 준비되어 있는 것

아래 데이터는 이미 계산이 끝나 있어서, 화면에 조회만 하면 됩니다 (실시간 계산/모델 추론 불필요):

- 단지별 평균 매매가, 위치(동)
- 단지별 가격 변동성 지표 (MDD/CV)
- 단지별 향후 1년 유동성 예측치
- 단지별 제안 LTV(%)

필요한 CSV/JSON 인터페이스는 정리해서 넘겨드릴 수 있습니다.

## 새로 구현해야 하는 것

- **지도 UI**: 지역 클릭 → 해당 지역 단지 리스트로 연결되는 인터랙션. 카카오맵 API 키는 이미 있음.
- **행정동 대신 법정동 단위 추천**: 행정동 경계로 나누려면 별도 매칭 작업이 필요해서, 지도는 법정동(답십리동, 장안동 등) 단위로 나누는 걸 추천 — UX 차이는 크지 않음.

## 주의할 점

- **일부 단지는 변동성 지표가 없음**: 거래 이력이 적은 단지(전체의 약 절반)는 가격 변동성 지표를 못 보여줌 → "거래 이력 부족으로 제공 불가" 같은 안내 필요
- **"오늘 시점"이 아니라 "최근 데이터 기준"**: 실시간 갱신이 아니라 데이터가 확보된 마지막 분기 기준 값이라는 걸 화면에 표기해야 함
- **세대수 미매칭 단지 존재**: 일부 단지는 LTV 계산 자체가 불가 → 별도 안내 필요

## 제안 순서

1. 데이터 인터페이스(CSV/JSON) 전달
2. 코멘트 문구 템플릿 전달 (수치를 자연어로 바꾸는 부분)
3. 지도 UI 구현
4. 리스트/상세 화면 구현 (순수 프론트엔드)
